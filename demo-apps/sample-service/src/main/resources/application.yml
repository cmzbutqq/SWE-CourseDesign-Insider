server:
  port: 8081

spring:
  application:
    name: sample-service

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      show-details: always
  metrics:
    tags:
      application: sample-service
